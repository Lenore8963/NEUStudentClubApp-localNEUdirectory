<!DOCTYPE html>
<html>
  <head>
    <title>User Profile</title>
  </head>
  <body>
    <h1>User Profile</h1>
    <p>Username: <%= user.preferred_username %></p>
    <!-- or 'user.name', depending on the token's claims -->
    <p>Display Name: <%= user.name %></p>
    <p>Email: <%= user.upn || user.email %></p>
    <!-- 'upn' for Azure AD's User Principal Name -->
  </body>
</html>
